import React from 'react';
import { Container } from 'react-bootstrap';
import './item.scss';

const Item = () => {
  return (
    <Container fluid>
      <div className="row">Item</div>
    </Container>
  );
};

export default Item;
