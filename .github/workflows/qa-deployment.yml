name: Continuous Deployment [QA]

on:
  push:
    branches:
      - main

env:
    BUILD_DIR: 'build/'
    AWS_REGION: 'us-west-1'
    QA_AWS_BUCKET_NAME: 
    QA_DISTRIBUTION_ID: 
    
jobs:
  deploy:
    name: Deploy QA
    
    uses: beckett-media/vault-react/.github/workflows/cont-deployment.yml@main
    with:
      AWS_REGION: us-west-1
      AWS_BUCKET_NAME: qa-vulcan-beckettvaulting-assets-548281136650
      CF_DISTRIBUTION_ID: E2K3QH80BXBBTA
    secrets: inherit